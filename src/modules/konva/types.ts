export type Position = {
  x?: number;
  y?: number;
};

export type WH = {
  width?: number;
  height?: number;
};

// Icon
export type IconType =
  | "eye"
  | "undo"
  | "plus"
  | "users"
  | "phone"
  | "random"
  | "archive"
  | "envelope"
  | "eye-slash"
  | "pencil-square-o";
